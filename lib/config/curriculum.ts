/**
 * Default curriculum the teacher UI loads into the upload form and
 * problem list. Phase 2 seeds only 'middle_3-1' with chapters and
 * problem_types — high_math1 / high_math2 exist as placeholder rows.
 * When additional curricula are seeded, turn this into a teacher-selectable
 * list in app/teacher/problems/new/page.tsx.
 */
export const DEFAULT_CURRICULUM_ID = 'middle_3-1';
